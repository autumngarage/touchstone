schema = 1

[validation]
runtime = "bash"

[[validation.targets]]
name = "root"
path = "."

[[validation.tasks]]
name = "self-tests"
target = "root"
command = "for test in tests/test-*.sh; do echo \"==> $test\"; bash \"$test\" || exit 1; done"
required = true
