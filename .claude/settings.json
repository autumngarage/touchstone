{
  "_comment": "Touchstone's own Claude Code settings. The branch guard is the one PreToolUse hook that survives the strip: it refuses `git commit` on the default branch at the tool boundary, before the commit is attempted. It points at hooks/branch-guard.sh directly — there is no longer a synced scripts/ copy, and nothing rewrites this file.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash",
            "args": ["${CLAUDE_PROJECT_DIR}/hooks/branch-guard.sh"]
          }
        ]
      }
    ]
  }
}
