# Secrets & config (NEVER commit these)
.env
.env.*
!.env.example
config.json
credentials.json
*.pem
*.key
*.p12

# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp
*.swo
*~

# Dependencies
node_modules/
vendor/
__pycache__/
*.pyc
.venv/
venv/

# Build output
dist/
build/
*.egg-info/
.next/
out/

# Claude Code project-local overrides (settings.json is platform-owned and committed;
# settings.local.json holds personal/host-specific customizations and is excluded by
# default — a team may opt to commit it by removing this line).
.claude/settings.local.json

# Touchstone
*.bak
