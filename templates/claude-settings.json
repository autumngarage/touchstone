{
  "_comment": "Claude Code settings for a Touchstone-aligned project. The branch guard refuses `git commit` on the default branch at the tool boundary, before the commit is attempted. Copy hooks/branch-guard.sh into the project alongside this file. User-specific overrides go in .claude/settings.local.json (gitignored), which Claude Code merges on top of this file.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR\"/scripts/branch-guard.sh"
          }
        ]
      }
    ]
  }
}
