# PR #697 merged — refactor: remove the worker engine (-5,700 lines)

**Date:** 2026-08-08
**Type:** pr-merged
**Trigger:** T1.9
**Cites:** journal/2026-08-08-remove-worker-engine
**Merge-commit:** a170b195e46d050b1d8bafda3b7e8e24bbc23c4c
**Branch:** refactor/cut-worker-engine

> refactor: remove the worker engine (-5,700 lines).

## What shipped

- scripts/worker.sh, lib/worker-ship-job.sh, lib/worker-review-fix.sh,
- the `touchstone worker` command, its usage block, and its
- worker entries from the bootstrap/update manifests and the
- [ ] Tests pass locally
- [ ] No new silent failures (no `except: pass`, no swallowed errors)
- [ ] No new code paths that diverge between environments
- [ ] Low risk — isolated change, no shared state affected
- [ ] Medium risk — touches shared infrastructure or data paths
- [ ] High risk — touches critical paths (see AGENTS.md for high-scrutiny list)

## Closes / advances

- **Plans:** _(none recorded — fill on edit)_
- **Doctrine:** _(none recorded — fill on edit)_
- **Journal linkage:** _(none recorded — fill on edit)_

## Follow-ups (deferred to future work)

_None._

(Per SPEC § 4.2, deferred items must resolve to another Plan, Journal entry, or Doctrine entry in the same commit as the merge note.)


<!--
Context auto-pulled at draft time. Remove this block before saving.

Recent commits:
- a170b19 refactor: remove the worker engine (-5,700 lines) (#697)
- 833f1c1 fix(delivery): fail-closed preflight, shape-preserving binary fingerprints (#691)
- 677ffe9 v2.12.1
- 7a52bc2 fix(delivery): harden synced scripts against downstream review findings (#679)
- ceb3036 v2.12.0

Active-branch PR: (no open PR for this branch)
-->

## Triggers fired

- T1.1 — diff touches `.cortex/doctrine/`, `.cortex/plans/`, `principles/`, or `SPEC.md` (files: principles/ai-delivery-architecture.md, principles/git-workflow.md)
- T1.4 — file deletion exceeds 100 lines (deleted 1208 from lib/worker-review-fix.sh) (files: lib/worker-review-fix.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 432 from lib/worker-ship-job.sh) (files: lib/worker-ship-job.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 168 from lib/worker-state.sh) (files: lib/worker-state.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 1635 from scripts/worker.sh) (files: scripts/worker.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 2326 from tests/test-worker.sh) (files: tests/test-worker.sh)
