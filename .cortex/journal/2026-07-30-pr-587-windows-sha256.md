# PR #587 merged — support Git for Windows SHA-256

**Date:** 2026-07-30
**Type:** pr-merged
**Trigger:** T1.9
**Cites:** GitHub issue #586, GitHub PR #587
**Merge-commit:** c53f37da52a7836d0811646904bda7696ddcf445
**Branch:** fix/windows-sha256-portability

> Touchstone moved delivery checksums behind one managed adapter and proved the
> path on Git for Windows.

## What shipped

- Add `lib/sha256.sh`, preferring `shasum` and falling back to `sha256sum`.
- Route preflight, merge authorization, update, and bootstrap hashing through
  the shared adapter.
- Propagate the helper through downstream manifests and dirty-write protection.
- Add a `windows-latest` Git Bash workflow plus hermetic fallback and
  missing-tool regressions.
- Preserve the host PATH in the preflight test harness so Git for Windows keeps
  access to `git.exe`.

## Closes / advances

- **Plans:** none
- **Doctrine:** none
- **Journal linkage:** none

## Follow-ups (deferred to future work)

_None._

(Per SPEC § 4.2, deferred items must resolve to another Plan, Journal entry, or
Doctrine entry in the same commit as the merge note.)
