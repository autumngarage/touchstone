# PR #295 merged — fix(update): install issue claim workflow

**Date:** 2026-05-09
**Type:** pr-merged
**Trigger:** T1.9
**Cites:** journal/2026-05-09-release-v2-11-3
**Merge-commit:** 4c336b17e173f9b8a422f382e315b122a502cea9
**Branch:** fix/install-claim-check-workflow

> fix(update): install issue claim workflow.

## What shipped

- Made the issue-claim workflow a Touchstone-managed artifact for bootstrap and update, closing the gap where downstream projects received docs that referenced claim enforcement but did not receive the workflow.
- Added an installable `templates/ci/issue-claim-check.yml` source so Homebrew/libexec installs do not depend on the repo's dot-directory `.github/` being present.
- Updated `principles/git-workflow.md` to document the three enforcement layers: claim script, local `open-pr.sh` preflight, and CI workflow backstop.
- Expanded sync-discipline planned-write coverage for newly managed claim-check paths and the existing `TOUCHSTONE.md` / `lib/preflight-scope.sh` managed paths, so dirty overlap checks protect local edits before update.
- Added regression coverage for bootstrap/update propagation, old-project recovery of missing claim files, workflow-template parity, and dirty overlap on the managed issue-claim workflow.
- Tests run: `pre-commit run --all-files`.
- Tests run: `bash tests/test-bootstrap.sh`.
- Tests run: `bash tests/test-update.sh`.
- Tests run: `bash tests/test-sync-scope-aware.sh`.
- Tests run: `for test in tests/test-*.sh; do bash "$test" || exit 1; done`.
- Merge gate: deterministic preflight passed.
- Merge gate: Conductor/OpenRouter review passed clean after blocking findings were addressed.
- Risk: high; this touched bootstrap/update and sync safety paths.

## Closes / advances

- **Plans:** none.
- **Doctrine:** none.
- **Journal linkage:** `journal/2026-05-09-release-v2-11-3`

## Follow-ups (deferred to future work)

_None._

(Per SPEC § 4.2, deferred items must resolve to another Plan, Journal entry, or Doctrine entry in the same commit as the merge note.)

## Triggers fired

- T1.1 — diff touches `.cortex/doctrine/`, `.cortex/plans/`, `principles/`, or `SPEC.md` (files: principles/git-workflow.md)
