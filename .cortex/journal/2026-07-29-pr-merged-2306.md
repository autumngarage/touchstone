# PR #554 merged — refactor: remove local model router

**Date:** 2026-07-29
**Type:** pr-merged
**Trigger:** T1.9
**Cites:** journal/2026-07-29-remove-local-model-router
**Merge-commit:** e5617d03a20b345a5e96a1e7273b5f6366936f29
**Branch:** refactor/remove-conductor

> refactor: remove local model router.

## What shipped

- Delete Conductor, local reviewer, routing, migration, and review-metrics code.
- Make GitHub Codex request and approval mandatory for every pushed PR head.
- Keep exact-head/base verification, unresolved-thread blocking, and deterministic preflight at merge.
- Update bootstrap, setup, steering, docs, completions, and regression tests.
- Full deterministic test suite run in segments.
- Focused open-PR and merge-gate tests pass.
- `pre-commit run --all-files` passes.
- Known timing-sensitive detached-worker failure is tracked separately in #553 and reproduced outside this refactor.

## Closes / advances

- **Plans:** none
- **Doctrine:** none
- **Journal linkage:** implements `journal/2026-07-29-remove-local-model-router`

## Follow-ups (deferred to future work)

_None._

(Per SPEC § 4.2, deferred items must resolve to another Plan, Journal entry, or Doctrine entry in the same commit as the merge note.)

## Triggers fired

- T1.1 — diff touches `.cortex/doctrine/`, `.cortex/plans/`, `principles/`, or `SPEC.md` (files: principles/agent-swarms.md, principles/ai-delivery-architecture.md, principles/file-upstream-bugs.md, principles/git-workflow.md)
- T1.4 — file deletion exceeds 100 lines (deleted 120 from bin/touchstone) (files: bin/touchstone)
- T1.4 — file deletion exceeds 100 lines (deleted 370 from bootstrap/migrate-review-config.sh) (files: bootstrap/migrate-review-config.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 485 from bootstrap/new-project.sh) (files: bootstrap/new-project.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 529 from bootstrap/review.sh) (files: bootstrap/review.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 159 from hooks/README.md) (files: hooks/README.md)
- T1.4 — file deletion exceeds 100 lines (deleted 228 from hooks/codex-review.config.example.toml) (files: hooks/codex-review.config.example.toml)
- T1.4 — file deletion exceeds 100 lines (deleted 5047 from hooks/codex-review.sh) (files: hooks/codex-review.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 240 from hooks/conductor-review.config.example.toml) (files: hooks/conductor-review.config.example.toml)
- T1.4 — file deletion exceeds 100 lines (deleted 345 from lib/review-comment.sh) (files: lib/review-comment.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 308 from lib/touchstone-doctor.sh) (files: lib/touchstone-doctor.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 5044 from scripts/codex-review.sh) (files: scripts/codex-review.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 346 from scripts/dogfood-agent-steering.sh) (files: scripts/dogfood-agent-steering.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 656 from scripts/merge-pr.sh) (files: scripts/merge-pr.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 184 from scripts/open-pr.sh) (files: scripts/open-pr.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 159 from setup.sh) (files: setup.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 240 from templates/setup.sh) (files: templates/setup.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 1009 from tests/test-advisory-at-pr-open.sh) (files: tests/test-advisory-at-pr-open.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 199 from tests/test-bootstrap.sh) (files: tests/test-bootstrap.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 254 from tests/test-codex-review-sentinel.sh) (files: tests/test-codex-review-sentinel.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 502 from tests/test-doctor.sh) (files: tests/test-doctor.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 230 from tests/test-events-json.sh) (files: tests/test-events-json.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 344 from tests/test-fixloop-findings-comment.sh) (files: tests/test-fixloop-findings-comment.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 317 from tests/test-merge-pr-dirty-tree-narrow.sh) (files: tests/test-merge-pr-dirty-tree-narrow.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 285 from tests/test-merge-pr-merge-state-retry.sh) (files: tests/test-merge-pr-merge-state-retry.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 857 from tests/test-merge-pr.sh) (files: tests/test-merge-pr.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 370 from tests/test-migrate-review-config.sh) (files: tests/test-migrate-review-config.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 181 from tests/test-preflight.sh) (files: tests/test-preflight.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 322 from tests/test-review-comment.sh) (files: tests/test-review-comment.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 647 from tests/test-review-dry-run.sh) (files: tests/test-review-dry-run.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 212 from tests/test-review-findings-checkpoint.sh) (files: tests/test-review-findings-checkpoint.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 4268 from tests/test-review-hook.sh) (files: tests/test-review-hook.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 161 from tests/test-review-trusted-base-config.sh) (files: tests/test-review-trusted-base-config.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 192 from tests/test-reviewer-sentinel-context.sh) (files: tests/test-reviewer-sentinel-context.sh)
- T1.4 — file deletion exceeds 100 lines (deleted 203 from tests/test-update-migrate-reviewer-config.sh) (files: tests/test-update-migrate-reviewer-config.sh)
